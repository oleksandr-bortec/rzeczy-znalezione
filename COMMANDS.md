# 💻 Rzeczy Znalezione - Всі команди для запуску

## 📋 Для колеги: Швидкий старт

### ✅ Крок 0: Перевір систему

```bash
# Перевір версію Node.js (потрібно >= 18.0.0)
node --version

# Перевір версію npm
npm --version
```

**Якщо Node.js не встановлений:**
- Mac: `brew install node`
- Windows: Завантаж з https://nodejs.org
- Linux: `sudo apt install nodejs npm` або `sudo yum install nodejs npm`

---

### 🚀 Крок 1: Завантаж проект

```bash
# Клонуй репозиторій
git clone https://github.com/oleksandr-bortec/rzeczy-znalezione.git

# Перейди в директорію
cd rzeczy-znalezione
```

---

### 📦 Крок 2: Встанови залежності

```bash
# Встанови всі пакети (автоматично налаштує проект!)
npm install
```

**Що відбудеться автоматично:**
- ✅ Створиться файл `.env` з безпечним ключем
- ✅ Створиться директорія `data/` з базою даних
- ✅ Додадуться 3 тестових користувачі
- ✅ Додадуться 16 демонстраційних предметів

**Очікуваний вивід:**
```
🚀 Rzeczy Znalezione - Automated Setup

═════════════════════════════════════════════════════════════
✅ Setup Complete!

🔑 Login Credentials:
   👑 Admin:    admin@example.com    / admin123
   👔 Official: official@example.com / official123
   👤 User:     user@example.com     / user123

🚀 Quick Start:
   npm run dev          - Start development server
═════════════════════════════════════════════════════════════
```

---

### ▶️ Крок 3: Запусти сервер

```bash
# Запуск в режимі розробки (автоперезавантаження)
npm run dev
```

**Очікуваний вивід:**
```
Initializing database...
Database initialized successfully
✅ Server running on http://localhost:3000
```

---

### 🌐 Крок 4: Відкрий у браузері

```bash
# Mac/Linux - відкрити в браузері автоматично
open http://localhost:3000

# Windows
start http://localhost:3000

# Або просто скопіюй в браузер:
http://localhost:3000
```

---

### 🔐 Крок 5: Увійди в систему

**Дані для входу:**

| Роль | Email | Пароль |
|------|-------|--------|
| Адміністратор | `admin@example.com` | `admin123` |
| Чиновник | `official@example.com` | `official123` |
| Користувач | `user@example.com` | `user123` |

**Сторінки:**
- Головна: http://localhost:3000
- Публічний пошук: http://localhost:3000/public.html
- Адмін панель: http://localhost:3000/admin.html
- Профіль: http://localhost:3000/profile.html

---

## 🔧 Команди для роботи з проектом

### Запуск сервера

```bash
# Розробка (з автоперезавантаженням при змінах)
npm run dev

# Продакшн (без автоперезавантаження)
npm start
```

### Робота з базою даних

```bash
# Перестворити базу даних (видалить всі дані!)
npm run init-db

# Ручне налаштування проекту
npm run setup
```

### Тестування

```bash
# Запустити тести
npm test
```

---

## 🛠️ Команди для вирішення проблем

### Проблема: "Порт 3000 зайнятий"

```bash
# Знайти процес на порту 3000
# Mac/Linux:
lsof -i :3000

# Windows:
netstat -ano | findstr :3000

# Вбити процес
# Mac/Linux:
kill -9 <PID>

# Windows:
taskkill /PID <PID> /F

# АБО змінити порт у .env
echo "PORT=3001" >> .env
npm run dev
```

### Проблема: "База даних не працює"

```bash
# Видалити стару базу
rm -rf data/

# Перестворити
npm run init-db

# Перезапустити сервер
npm run dev
```

### Проблема: "JWT_SECRET відсутній"

```bash
# Перевірити .env
cat .env | grep JWT_SECRET

# Якщо порожній, перестворити
rm .env
npm run setup
```

### Проблема: "Старий кеш у браузері"

**Варіант 1 - В консолі браузера (F12):**
```javascript
await window.clearAllCaches();
```

**Варіант 2 - Спеціальна сторінка:**
```
http://localhost:3000/clear-cache.html
```

**Варіант 3 - Комбінація клавіш:**
```
Ctrl + Shift + R  (Windows/Linux)
Cmd + Shift + R   (Mac)
```

### Проблема: "npm install не працює"

```bash
# Очистити кеш npm
npm cache clean --force

# Видалити node_modules
rm -rf node_modules package-lock.json

# Перевстановити
npm install
```

---

## 🔄 Git команди

### Отримати останні зміни

```bash
# Завантажити зміни
git pull

# Оновити залежності (якщо змінився package.json)
npm install

# Перезапустити сервер
npm run dev
```

### Відправити свої зміни

```bash
# Подивитись що змінилось
git status

# Додати всі зміни
git add .

# Створити коміт
git commit -m "Опис змін"

# Відправити на GitHub
git push
```

### Створити нову гілку

```bash
# Створити і перейти на нову гілку
git checkout -b feature/my-feature

# Відправити нову гілку на GitHub
git push -u origin feature/my-feature
```

---

## 🔍 Корисні команди для розробки

### Перевірка статусу

```bash
# Статус git
git status

# Статус серверу (якщо запущений у фоні)
ps aux | grep "node server"

# Перевірка портів
# Mac/Linux:
lsof -i -P -n | grep LISTEN

# Windows:
netstat -ano | findstr LISTENING
```

### Перегляд логів

```bash
# Запустити з детальними логами
npm run dev

# Запустити з збереженням логів у файл
npm run dev 2>&1 | tee server.log
```

### Перегляд бази даних

```bash
# Встановити SQLite CLI (якщо потрібно)
# Mac:
brew install sqlite

# Ubuntu/Debian:
sudo apt install sqlite3

# Відкрити базу
sqlite3 data/rzeczy-znalezione.db

# SQLite команди:
# .tables          - показати таблиці
# .schema users    - структура таблиці users
# SELECT * FROM items LIMIT 5;  - перші 5 предметів
# .quit            - вийти
```

### Перегляд .env файлу

```bash
# Показати всі змінні
cat .env

# Показати тільки JWT_SECRET
cat .env | grep JWT_SECRET

# Показати PORT
cat .env | grep PORT
```

### Очищення проекту

```bash
# Видалити всі згенеровані файли
rm -rf node_modules data .env

# Перезапустити налаштування
npm install
```

---

## 📊 Корисні API endpoints

```bash
# Перевірка здоров'я сервера
curl http://localhost:3000/api/health

# Перевірка версії
curl http://localhost:3000/api/version

# Статистика
curl http://localhost:3000/api/stats

# Список предметів (потрібна авторизація)
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/items
```

---

## 🎯 Швидкі сценарії

### Сценарій 1: Повне перезавантаження

```bash
# Зупинити сервер (Ctrl+C)
# Видалити все
rm -rf node_modules data .env
# Перевстановити
npm install
# Запустити
npm run dev
```

### Сценарій 2: Оновлення з GitHub

```bash
# Отримати зміни
git pull
# Оновити залежності
npm install
# Перезапустити
npm run dev
```

### Сценарій 3: Створення бекапу

```bash
# Створити директорію для бекапів
mkdir -p backups

# Зберегти базу даних
cp data/rzeczy-znalezione.db backups/backup-$(date +%Y%m%d-%H%M%S).db

# Зберегти .env
cp .env backups/env-$(date +%Y%m%d-%H%M%S).txt
```

### Сценарій 4: Відновлення з бекапу

```bash
# Зупинити сервер (Ctrl+C)

# Відновити базу
cp backups/backup-20250107-143000.db data/rzeczy-znalezione.db

# Відновити .env
cp backups/env-20250107-143000.txt .env

# Запустити
npm run dev
```

---

## 🐛 Діагностика проблем

### Чеклист якщо щось не працює:

1. **Перевір версію Node.js**
   ```bash
   node --version  # Має бути >= 18.0.0
   ```

2. **Перевір чи існує .env**
   ```bash
   ls -la .env
   cat .env
   ```

3. **Перевір чи існує база даних**
   ```bash
   ls -la data/rzeczy-znalezione.db
   ```

4. **Перевір чи порт вільний**
   ```bash
   # Mac/Linux:
   lsof -i :3000

   # Windows:
   netstat -ano | findstr :3000
   ```

5. **Перевір логи сервера**
   ```bash
   npm run dev
   # Читай помилки в консолі
   ```

6. **Перевір браузерну консоль**
   ```
   F12 → Console
   # Читай помилки JavaScript
   ```

---

## 📚 Додаткова документація

- **QUICK-SETUP.md** - Швидкий старт (3 кроки)
- **SETUP-GUIDE.md** - Повний посібник з налаштування
- **ROZWIAZANIE-PROBLEMOW.md** - Вирішення проблем
- **HACKNATION-README.md** - Функціонал проекту
- **START_HERE.md** - Перші кроки

---

## 💡 Підказки

### Mac/Linux користувачам:

```bash
# Зробити скрипт виконуваним
chmod +x script.sh

# Видалення файлів
rm -rf folder/

# Перегляд процесів
ps aux | grep node
```

### Windows користувачам:

```bash
# Використовуй Git Bash або PowerShell

# Видалення файлів
rmdir /s /q folder

# Перегляд процесів
tasklist | findstr node
```

### Алиаси для швидкості (додай в ~/.bashrc або ~/.zshrc):

```bash
# Швидкий запуск
alias rz-dev='cd ~/rzeczy-znalezione && npm run dev'
alias rz-reset='cd ~/rzeczy-znalezione && rm -rf data .env && npm run setup'
alias rz-backup='cp ~/rzeczy-znalezione/data/rzeczy-znalezione.db ~/backups/rz-$(date +%Y%m%d).db'
```

---

## ❓ Частi питання

**Q: Як змінити мову інтерфейсу?**
A: Клікни на прапорець PL/EN в правому верхньому куті

**Q: Як додати нового користувача?**
A: Увійди як admin → Admin Panel → Users → Add User

**Q: Як експортувати дані?**
A: Головна сторінка → Крок 4 → Download JSON/CSV

**Q: Як імпортувати Excel?**
A: Головна сторінка → Крок 1 → Drag & Drop або Click to Upload

**Q: Де знаходяться логи?**
A: В консолі де запущено `npm run dev`

**Q: Як змінити порт?**
A: Відредагуй `.env` файл, змінни `PORT=3001`

---

## 🆘 Потрібна допомога?

1. ✅ Перечитай цей документ
2. ✅ Перевір [SETUP-GUIDE.md](SETUP-GUIDE.md)
3. ✅ Подивись Issues на GitHub
4. ✅ Запитай в команді

---

**Версія:** 2.0.6
**Оновлено:** 2025-12-07
**Готово до використання!** 🚀
