# 🚀 Rzeczy Znalezione - Повний посібник з налаштування

## 📋 Зміст

1. [Швидкий старт](#швидкий-старт)
2. [Детальна інструкція](#детальна-інструкція)
3. [Автоматичне оновлення кешу](#автоматичне-оновлення-кешу)
4. [Вирішення проблем](#вирішення-проблем)
5. [Перенесення між середовищами](#перенесення-між-середовищами)

---

## 🎯 Швидкий старт

### Для нових користувачів (перше завантаження)

```bash
# 1. Клонуємо репозиторій
git clone <repository-url>
cd rzeczy-znalezione

# 2. Встановлюємо залежності (автоматично запуститься налаштування)
npm install

# 3. Запускаємо сервер
npm run dev
```

**Готово!** Відкрийте http://localhost:3000

### Що відбулося автоматично?

✅ Створено файл `.env` з безпечним JWT ключем
✅ Створено директорію `data/`
✅ Ініціалізовано базу даних SQLite
✅ Додано 3 тестових користувачів
✅ Додано 16 демонстраційних предметів

---

## 🔧 Детальна інструкція

### 1️⃣ Системні вимоги

- **Node.js**: >= 18.0.0
- **npm**: >= 8.0.0
- **Операційна система**: Windows, macOS, Linux

### 2️⃣ Встановлення

#### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/your-username/rzeczy-znalezione.git
cd rzeczy-znalezione
```

#### Крок 2: Встановлення залежностей

```bash
npm install
```

**Що відбувається під капотом:**

1. npm встановлює всі пакети з `package.json`
2. Запускається `postinstall` hook → викликає `setup.js`
3. `setup.js` виконує:
   - Перевіряє наявність `.env` → створює з `.env.example`
   - Генерує унікальний `JWT_SECRET` (64 байти криптографічно безпечний ключ)
   - Створює директорію `data/`
   - Запускає `init-db.js` для створення бази даних
   - Виводить облікові дані для входу

#### Крок 3: Перевірка налаштування

```bash
# Перевірити, чи створено .env
cat .env

# Перевірити, чи створено базу даних
ls -lh data/rzeczy-znalezione.db
```

### 3️⃣ Конфігурація (.env файл)

Файл `.env` автоматично створюється з наступними параметрами:

```env
# Сервер
PORT=3000
NODE_ENV=development

# Безпека (автоматично згенерований)
JWT_SECRET=<randomly-generated-64-byte-hex-string>
JWT_EXPIRES_IN=7d

# База даних
DATABASE_PATH=./data/database.sqlite

# API dane.gov.pl
DANE_GOV_API_URL=https://api.dane.gov.pl
DANE_GOV_API_VERSION=1.4

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000       # 15 хвилин
RATE_LIMIT_MAX_REQUESTS=100       # 100 запитів на вікно

# Адміністратор (за замовчуванням)
ADMIN_EMAIL=admin@rzeczy-znalezione.gov.pl
ADMIN_PASSWORD=AdminHackNation2025!
```

**Важливо:** `JWT_SECRET` генерується випадково для кожної інсталяції!

### 4️⃣ Структура бази даних

База даних SQLite створюється автоматично з 5 таблицями:

#### Таблиця: `users`
- Зберігає облікові записи користувачів
- Паролі хешуються з bcrypt (cost factor: 12)
- Ролі: `admin`, `official`, `user`

#### Таблиця: `items`
- Зберігає предмети, що знайдено
- 20+ полів (назва, категорія, GPS координати, статус, тощо)
- Індекси на `status`, `category`, `voivodeship`, `date_found`, `public_id`

#### Таблиця: `audit_log`
- Журнал активності користувачів
- Зберігає всі зміни (CRUD операції)

#### Таблиця: `sessions`
- Управління JWT токенами
- Автоматичне видалення протермінованих сесій

#### Таблиця: `stats_cache`
- Кешування статистики
- Автоматичне оновлення кожні 30 секунд

### 5️⃣ Тестові дані

При першій ініціалізації створюються:

**3 користувачі:**
```
👑 Адміністратор:
   Email: admin@example.com
   Пароль: admin123
   Права: повний доступ

👔 Чиновник:
   Email: official@example.com
   Пароль: official123
   Права: додавання/редагування предметів

👤 Користувач:
   Email: user@example.com
   Пароль: user123
   Права: перегляд предметів
```

**16 демонстраційних предметів:**
- Розподілені по 9 містах Польщі
- Різні категорії (телефони, гаманці, ключі, документи, тощо)
- Реалістичні GPS координати
- Різні статуси (stored, returned, liquidated)

---

## 🔄 Автоматичне оновлення кешу

### Як це працює?

Система автоматичного оновлення складається з 3 компонентів:

#### 1. Service Worker (sw.js)

**Версія:** 2.0.6

**Функціонал:**
```javascript
// Перевірка оновлень кожні 5 хвилин
const UPDATE_CHECK_INTERVAL = 5 * 60 * 1000;

// Автоматична перевірка версії
fetch('/api/version') → порівняння з поточною версією
→ якщо є нова → повідомлення користувача
→ автоматична активація нового SW
```

**Стратегії кешування:**

| Тип ресурсу | Стратегія | Опис |
|-------------|-----------|------|
| Статичні файли (.css, .js, .svg) | Cache-first | Швидке завантаження |
| API запити (/api/*) | Network-first | Актуальні дані |
| HTML сторінки | Network-first | Свіжий контент |
| Зовнішні CDN | Cache-first | Google Fonts, FontAwesome |

**Development vs Production:**

```javascript
// Development (localhost)
- Кешування ВИМКНЕНО
- Завжди мережа → fresh content
- Швидше тестування змін

// Production
- Кешування АКТИВНО
- Автоматичні оновлення
- Offline підтримка
```

#### 2. API Endpoint (/api/version)

**Відповідь:**
```json
{
  "version": "2.0.6",
  "timestamp": 1234567890,
  "serviceWorker": "2.0.6"
}
```

**Cache-Control:**
```
Cache-Control: no-store, no-cache, must-revalidate
```
Завжди свіжі дані про версію!

#### 3. Update Handler (sw-update-handler.js)

**Функції:**

✅ Автоматична реєстрація Service Worker
✅ Перевірка оновлень кожну годину
✅ Красиве повідомлення про нову версію
✅ Кнопки "Оновити зараз" / "Пізніше"
✅ Автоматичне перезавантаження після активації

**Глобальні функції:**

```javascript
// Ручна перевірка оновлень
window.checkForUpdates();

// Повне очищення кешу + Service Worker
window.clearAllCaches();
```

### Використання в консолі браузера

```javascript
// Перевірити, чи є оновлення
await checkForUpdates();

// Очистити весь кеш і перезавантажити
await clearAllCaches();

// Перевірити поточну версію SW
navigator.serviceWorker.controller.scriptURL
```

### Візуальне повідомлення

При наявності нової версії з'являється банер:

```
┌─────────────────────────────────────────────────┐
│ 🔄 Нова версія доступна!                        │
│ Оновіть сторінку для кращої роботи.            │
│                                                  │
│ [🔄 Оновити зараз]  [Пізніше]                  │
└─────────────────────────────────────────────────┘
```

**Стилізація:**
- Градієнтний фон (#667eea → #764ba2)
- Анімація з'явлення (slideDown)
- Іконка обертається (spin animation)
- Кнопки з hover ефектом

---

## 🛠️ Вирішення проблем

### Проблема 1: База даних не створюється

**Симптом:**
```
Error: SQLITE_CANTOPEN: unable to open database file
```

**Рішення:**
```bash
# Створити директорію data вручну
mkdir -p data

# Перезапустити ініціалізацію
npm run init-db
```

### Проблема 2: JWT_SECRET відсутній

**Симптом:**
```
Error: JWT_SECRET is not defined
```

**Рішення:**
```bash
# Перевірити .env файл
cat .env | grep JWT_SECRET

# Якщо порожній, згенерувати вручну
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Додати до .env:
JWT_SECRET=<generated-secret>
```

### Проблема 3: Порт 3000 зайнятий

**Симптом:**
```
Error: listen EADDRINUSE :::3000
```

**Рішення:**
```bash
# Змінити порт у .env
echo "PORT=3001" >> .env

# Або знайти процес
lsof -i :3000
kill -9 <PID>
```

### Проблема 4: Старий кеш після оновлення

**Симптом:**
- Бачите стару версію сайту
- Зміни не відображаються

**Рішення 1 - Автоматичне:**
```javascript
// У консолі браузера
await window.clearAllCaches();
```

**Рішення 2 - Ручне:**
1. Відкрийте DevTools (F12)
2. Application → Storage → Clear site data
3. Або Ctrl+Shift+R (жорстке перезавантаження)

**Рішення 3 - Інструмент:**
Відкрийте: http://localhost:3000/clear-cache.html

### Проблема 5: Service Worker не оновлюється

**Симптом:**
- Банер оновлення не з'являється
- Версія SW застаріла

**Діагностика:**
```javascript
// Перевірити активний SW
navigator.serviceWorker.controller

// Перевірити версію
navigator.serviceWorker.controller.scriptURL

// Перевірити стан реєстрації
navigator.serviceWorker.getRegistration().then(reg => {
  console.log('Active:', reg.active?.scriptURL);
  console.log('Waiting:', reg.waiting?.scriptURL);
  console.log('Installing:', reg.installing?.scriptURL);
});
```

**Рішення:**
```javascript
// Примусове оновлення
const reg = await navigator.serviceWorker.getRegistration();
await reg.update();

// Або скасувати реєстрацію
await reg.unregister();
location.reload();
```

---

## 📦 Перенесення між середовищами

### Сценарій 1: Development → Production

#### Крок 1: Експорт даних

```bash
# Скопіювати базу даних
cp data/rzeczy-znalezione.db backup-$(date +%Y%m%d).db

# Або експортувати в JSON
node -e "
  const db = require('./server/database');
  const items = db.prepare('SELECT * FROM items').all();
  require('fs').writeFileSync('items-export.json', JSON.stringify(items, null, 2));
"
```

#### Крок 2: Налаштування production

```bash
# На production сервері
git clone <repository-url>
cd rzeczy-znalezione

# Встановлення
npm install

# Редагувати .env
nano .env
# Змінити:
NODE_ENV=production
PORT=80
JWT_SECRET=<new-secure-secret>
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=<strong-password>
```

#### Крок 3: Імпорт даних

```bash
# Скопіювати базу
scp backup-20250107.db server:/path/to/rzeczy-znalezione/data/rzeczy-znalezione.db

# Або імпортувати JSON
# (створити скрипт імпорту або використати admin panel)
```

### Сценарій 2: Між розробниками

#### Підхід 1: З даними (для демо/тестування)

```bash
# Розробник 1 (експорт)
git add .
git commit -m "Update features"
git push

# Зберегти базу окремо (не в git!)
cp data/rzeczy-znalezione.db ~/shared-folder/

# Розробник 2 (імпорт)
git pull
npm install                           # Автоматично створить порожню БД
cp ~/shared-folder/rzeczy-znalezione.db data/
npm run dev
```

#### Підхід 2: Без даних (чисте середовище)

```bash
# Розробник 2
git pull
npm install        # Автоматично створить тестові дані
npm run dev
```

### Сценарій 3: CI/CD Pipeline

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci
        env:
          CI: true  # Пропустить автоматичний setup

      - name: Setup production
        run: |
          cp .env.example .env
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env
          echo "NODE_ENV=production" >> .env

      - name: Initialize database (if needed)
        run: npm run init-db

      - name: Deploy to server
        # ... ваш deployment скрипт
```

---

## 📊 Моніторинг і логування

### Логи Service Worker

```javascript
// У консолі браузера
navigator.serviceWorker.addEventListener('message', (event) => {
  console.log('SW Message:', event.data);
});

// Подивитись всі SW події
// DevTools → Application → Service Workers → Console
```

### Логи сервера

```bash
# Запустити з виводом логів
npm run dev

# Або з збереженням у файл
npm run dev 2>&1 | tee server.log
```

### Статистика бази даних

```bash
# Розмір бази
ls -lh data/rzeczy-znalezione.db

# Кількість записів
node -e "
  const db = require('./server/database');
  console.log('Users:', db.prepare('SELECT COUNT(*) as c FROM users').get().c);
  console.log('Items:', db.prepare('SELECT COUNT(*) as c FROM items').get().c);
  console.log('Audit logs:', db.prepare('SELECT COUNT(*) as c FROM audit_log').get().c);
"
```

---

## 🔒 Безпека

### Рекомендації для production

1. **Змінити облікові дані за замовчуванням**
   ```sql
   -- Видалити тестових користувачів
   DELETE FROM users WHERE email LIKE '%@example.com';

   -- Створити реального адміністратора через UI
   ```

2. **Використовувати сильний JWT_SECRET**
   ```bash
   # Згенерувати 128-байтний ключ
   node -e "console.log(require('crypto').randomBytes(128).toString('hex'))"
   ```

3. **Налаштувати HTTPS**
   ```bash
   # Використати nginx reverse proxy
   # Або Cloudflare SSL
   ```

4. **Обмежити CORS**
   ```javascript
   // server/index.js
   app.use(cors({
     origin: 'https://your-domain.com'
   }));
   ```

5. **Включити rate limiting**
   ```env
   RATE_LIMIT_MAX_REQUESTS=50  # Знизити для production
   ```

---

## 📝 Корисні команди

```bash
# Встановлення
npm install                    # Автоматичне налаштування
npm run setup                  # Ручне налаштування

# Розробка
npm run dev                    # Сервер з автоперезавантаженням
npm start                      # Production сервер

# База даних
npm run init-db                # Ініціалізація/скидання БД

# Тестування
npm test                       # Запуск тестів

# Очищення
rm -rf data/                   # Видалити базу
rm .env                        # Видалити конфігурацію
npm run setup                  # Перестворити
```

---

## 🎓 Додаткові ресурси

- **Документація проекту:** [HACKNATION-README.md](HACKNATION-README.md)
- **Швидкий старт:** [START_HERE.md](START_HERE.md)
- **API документація:** http://localhost:3000/api/health
- **Інструмент очищення кешу:** http://localhost:3000/clear-cache.html

---

## 💬 Підтримка

Якщо виникли проблеми:

1. ✅ Перевірте цей посібник
2. ✅ Прочитайте [START_HERE.md](START_HERE.md)
3. ✅ Перегляньте Issues на GitHub
4. ✅ Створіть новий Issue з описом проблеми

---

**Останнє оновлення:** 2025-12-07
**Версія:** 2.0.6
